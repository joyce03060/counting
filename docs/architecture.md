# 记账app — 架构设计文档

> ⚠️ 本文档待技术栈选定后填写完整。

---

## 技术选型状态

| 决策项 | 状态 | 选定方案 |
|--------|------|----------|
| 桌面框架 | 🔴 待用户选择 | — |
| 前端 UI 框架 | 🔴 待用户选择 | — |
| 编程语言 | 🔴 待用户选择 | — |
| 数据存储方案 | 🔴 待用户选择 | — |
| UI 组件库 | 🔴 待用户选择 | — |
| 图表库 | 🔴 待用户选择 | — |

---

## 架构概览（待填写）

选定技术栈后将在此处记录：

- 整体架构分层
- 数据流设计
- 组件树结构
- 路由设计
- 状态管理方案

---

## 数据流设计（待填写）

记账应用的核心数据流：

```
用户输入 → 表单验证 → 数据存储 → 列表刷新 → 统计更新
```

---

## 目录结构规划（待填写）

```
src/
├── main/           # 主进程/入口
├── renderer/       # 界面渲染
│   ├── components/ # UI 组件
│   ├── pages/      # 页面
│   ├── hooks/      # 逻辑复用
│   └── styles/     # 样式
├── shared/         # 共享代码
│   ├── types/      # 类型定义
│   └── utils/      # 工具函数
└── data/           # 数据层
    └── store/      # 数据存储
```
